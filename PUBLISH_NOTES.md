# Publish Notes

Before publishing:

- Choose and finalize the license in `LICENSE.md`.
- Update `thunderstore.toml` namespace, description, and website URL.
- Build once against the target V Rising / VampireReferenceAssemblies version.
- Test `.csw validate` on a fresh server config.
- Keep `DebugLogging` set to `false` in public examples.
